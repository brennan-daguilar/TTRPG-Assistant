services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: ttrpg
      POSTGRES_PASSWORD: ttrpg_dev
      POSTGRES_DB: ttrpghelper
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
